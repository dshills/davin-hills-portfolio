{
  "name": "davin-hills-portfolio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "fetch-articles": "node scripts/fetch-articles.mjs",
    "fetch-repos": "node scripts/fetch-repos.mjs",
    "fetch-data": "npm run fetch-articles && npm run fetch-repos",
    "build": "npm run fetch-data && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.0.0"
  }
}
